--[[
    LEMDIKLAT PANEL V2 - AUTH BYPASS EXPLOIT
    Semua remote bisa dipanggil tanpa perlu TabletPelatihan
]]

repeat task.wait() until game:IsLoaded()

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

-- Cari semua remote
local ToggleLockpad = ReplicatedStorage:WaitForChild("ToggleLockpad", 5)
local GetLockpadStatus = ReplicatedStorage:WaitForChild("GetLockpadStatus", 5)
local GetStudents = ReplicatedStorage:WaitForChild("GetStudents", 5)
local SetStudentTraining = ReplicatedStorage:WaitForChild("SetStudentTraining", 5)

-- Cek apakah semua remote ditemukan
local allFound = ToggleLockpad and GetLockpadStatus and GetStudents and SetStudentTraining
if not allFound then
    warn("[!] Beberapa remote tidak ditemukan!")
    return
end

print("========== LEMDIKLAT EXPLOIT ==========")
print("Semua remote ditemukan. Membuat GUI...")

-- Buat GUI sederhana
local gui = Instance.new("ScreenGui")
gui.Name = "LemdiklatExploit"
gui.ResetOnSpawn = false
gui.DisplayOrder = 999999
gui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 350, 0, 450)
frame.Position = UDim2.new(0.5, -175, 0.5, -225)
frame.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 8)
corner.Parent = frame

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(200, 150, 30)
stroke.Thickness = 1.5
stroke.Parent = frame

-- Title
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 36)
title.BackgroundColor3 = Color3.fromRGB(200, 150, 30)
title.BackgroundTransparency = 0.2
title.Text = "🔓 LEMDIKLAT EXPLOIT"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 14
title.Parent = frame

local tCorner = Instance.new("UICorner")
tCorner.CornerRadius = UDim.new(0, 8)
tCorner.Parent = title

-- Content
local content = Instance.new("ScrollingFrame")
content.Size = UDim2.new(1, -16, 1, -48)
content.Position = UDim2.new(0, 8, 0, 42)
content.BackgroundTransparency = 1
content.BorderSizePixel = 0
content.ScrollBarThickness = 4
content.CanvasSize = UDim2.new(0, 0, 0, 600)
content.AutomaticCanvasSize = Enum.AutomaticSize.Y
content.Parent = frame

-- Section: Status Pad
local function addLabel(text, color)
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, 0, 0, 22)
    lbl.BackgroundTransparency = 1
    lbl.Text = text
    lbl.TextColor3 = color or Color3.fromRGB(220, 220, 220)
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 13
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = content
    return lbl
end

local function addButton(text, color, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 32)
    btn.BackgroundColor3 = color or Color3.fromRGB(60, 60, 60)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 12
    btn.Parent = content
    
    local bCorner = Instance.new("UICorner")
    bCorner.CornerRadius = UDim.new(0, 6)
    bCorner.Parent = btn
    
    btn.MouseButton1Click:Connect(callback)
    return btn
end

local function addTextBox(placeholder, default)
    local box = Instance.new("TextBox")
    box.Size = UDim2.new(1, 0, 0, 30)
    box.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    box.TextColor3 = Color3.fromRGB(255, 255, 255)
    box.PlaceholderText = placeholder
    box.PlaceholderColor3 = Color3.fromRGB(100, 100, 110)
    box.Text = default or ""
    box.Font = Enum.Font.Gotham
    box.TextSize = 12
    box.ClearTextOnFocus = false
    box.Parent = content
    
    local bCorner = Instance.new("UICorner")
    bCorner.CornerRadius = UDim.new(0, 4)
    bCorner.Parent = box
    return box
end

addLabel("⚡ PAD CONTROL", Color3.fromRGB(200, 150, 30))

-- Pad selector
local padBox = addTextBox("Pad ID (1-6)", "1")
local studentNameBox = addTextBox("Nama Siswa (opsional)", "")
local trainingTypeBox = addTextBox("Tipe Training", "SECATAM")

addButton("🔒 LOCK PAD", Color3.fromRGB(180, 30, 30), function()
    local pad = tonumber(padBox.Text) or 1
    local name = studentNameBox.Text
    local training = trainingTypeBox.Text
    if name == "" then name = nil end
    
    local success, err = pcall(function()
        ToggleLockpad:FireServer(pad, true, name, training)
    end)
    print("[LOCK] Pad", pad, ":", success, err or "OK")
end)

addButton("🔓 UNLOCK PAD", Color3.fromRGB(30, 130, 40), function()
    local pad = tonumber(padBox.Text) or 1
    local success, err = pcall(function()
        ToggleLockpad:FireServer(pad, false, nil, nil)
    end)
    print("[UNLOCK] Pad", pad, ":", success, err or "OK")
end)

addLabel("", Color3.fromRGB(0, 0, 0)) -- spacer
addLabel("🎓 STUDENT CONTROL", Color3.fromRGB(200, 150, 30))

local targetStudentBox = addTextBox("Nama Siswa Target", "")

addButton("✅ SET TRAINING", Color3.fromRGB(30, 130, 40), function()
    local name = targetStudentBox.Text
    local pad = tonumber(padBox.Text) or 1
    local training = trainingTypeBox.Text
    
    if name == "" then
        print("[!] Masukkan nama siswa!")
        return
    end
    
    local success, err = pcall(function()
        SetStudentTraining:FireServer(name, true, pad, training)
    end)
    print("[TRAINING]", name, ":", success, err or "OK")
end)

addButton("❌ REMOVE TRAINING", Color3.fromRGB(180, 30, 30), function()
    local name = targetStudentBox.Text
    if name == "" then
        print("[!] Masukkan nama siswa!")
        return
    end
    
    local success, err = pcall(function()
        SetStudentTraining:FireServer(name, false, nil, nil)
    end)
    print("[UN-TRAINING]", name, ":", success, err or "OK")
end)

addLabel("", Color3.fromRGB(0, 0, 0))
addLabel("📊 DATA DUMP", Color3.fromRGB(200, 150, 30))

local logBox = Instance.new("TextLabel")
logBox.Size = UDim2.new(1, 0, 0, 120)
logBox.BackgroundColor3 = Color3.fromRGB(12, 12, 18)
logBox.TextColor3 = Color3.fromRGB(200, 200, 200)
logBox.Font = Enum.Font.Gotham
logBox.TextSize = 10
logBox.TextWrapped = true
logBox.TextXAlignment = Enum.TextXAlignment.Left
logBox.TextYAlignment = Enum.TextYAlignment.Top
logBox.Text = "Klik tombol di bawah untuk dump data..."
logBox.Parent = content

local logCorner = Instance.new("UICorner")
logCorner.CornerRadius = UDim.new(0, 4)
logCorner.Parent = logBox

addButton("📋 DUMP STATUS PAD", Color3.fromRGB(60, 60, 80), function()
    local success, data = pcall(function()
        return GetLockpadStatus:InvokeServer()
    end)
    
    if success and data then
        local output = "STATUS PAD:\n"
        for padId, info in pairs(data) do
            local locked = info.locked and "🔒 LOCKED" or "🔓 OPEN"
            output = output .. "  PAD " .. padId .. ": " .. locked .. "\n"
        end
        logBox.Text = output
        print("[DUMP] Status pad:", data)
    else
        logBox.Text = "Gagal mengambil data status pad"
    end
end)

addButton("📋 DUMP SISWA", Color3.fromRGB(60, 60, 80), function()
    local success, students = pcall(function()
        return GetStudents:InvokeServer()
    end)
    
    if success and students then
        local output = "SISWA:\n"
        for _, s in pairs(students) do
            local status = s.inTraining and "✅ Training" or "⬜ Idle"
            output = output .. "  " .. s.displayName .. " (@" .. s.name .. ") [" .. s.userId .. "] " .. status .. "\n"
        end
        logBox.Text = output
        print("[DUMP] Students:", students)
    else
        logBox.Text = "Gagal mengambil data siswa"
    end
end)

addLabel("", Color3.fromRGB(0, 0, 0))
addLabel("💡 Tekan P untuk toggle panel ori", Color3.fromRGB(120, 120, 130))

-- Quick actions
addLabel("", Color3.fromRGB(0, 0, 0))
addLabel("⚡ QUICK ACTIONS", Color3.fromRGB(200, 100, 30))

addButton("🔒 LOCK ALL PADS", Color3.fromRGB(180, 20, 20), function()
    for i = 1, 6 do
        pcall(function()
            ToggleLockpad:FireServer(i, true, nil, "SECATAM")
        end)
        task.wait(0.1)
    end
    print("[!] Semua pad di-LOCK")
end)

addButton("🔓 UNLOCK ALL PADS", Color3.fromRGB(20, 120, 30), function()
    for i = 1, 6 do
        pcall(function()
            ToggleLockpad:FireServer(i, false, nil, nil)
        end)
        task.wait(0.1)
    end
    print("[!] Semua pad di-UNLOCK")
end)

print("========================================")
print("  LEMDIKLAT EXPLOIT READY")
print("  Gunakan GUI di layar")
print("  Data dump via console (F9)")
print("========================================")
